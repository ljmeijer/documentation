How image and text of the [[GUIContent]] is combined.
CONVERTEXAMPLE
BEGIN EX

	// Prints how image and text is placed inside customStyles[0]

	function OnGUI() {
		if(GUI.skin.customStyles.Length > 0)
			Debug.Log(GUI.skin.customStyles[0].imagePosition);
	}
END EX