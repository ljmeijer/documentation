Center of the occlusion area relative to the transform. Occlusion is blalbal very important bla you should use it.
and we should write docs for it.
CONVERTEXAMPLE
BEGIN EX

	// Sets the center of the occlusion area to the center of the transform

	@script RequireComponent(OcclusionArea)

	transform.GetComponent(OcclusionArea).center = Vector3.zero;
END EX

BEGIN EX
function Start()
{
  Debug.Log("dowit");
}
END EX