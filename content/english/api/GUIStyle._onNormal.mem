Rendering settings for when the control is turned on.
CONVERTEXAMPLE
BEGIN EX

	function OnGUI() {
		if(GUI.skin.customStyles.Length > 0)
			GUI.skin.customStyles[0].onNormal.textColor = Color.red;		
	}
END EX