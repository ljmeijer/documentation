Pixel distance from mouse pointer to a 3D disc.

Calculates the screen space distance from the mouse pointer to the disc (circle) at given world
space /position/ with the given /radius/ and /normal/.

Uses the current camera to determine the distance.

SA: ::ref::DistanceToCircle.