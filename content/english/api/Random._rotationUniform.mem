Returns a random rotation with uniform distribution(RO).
CONVERTEXAMPLE
BEGIN EX

	// Instantiates a prefab with a random rotation at the world-origin

	var prefab : GameObject;
	Instantiate(prefab, Vector3.zero, Random.rotationUniform);
END EX