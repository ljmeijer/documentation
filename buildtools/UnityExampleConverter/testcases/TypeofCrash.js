/*
using UnityEngine;
using System.Collections;

public class TypeofCrash : MonoBehaviour {
    void Awake() {
        Debug.Log(typeof(Material));
    }
}
*/
/*
import UnityEngine
import System.Collections

class TypeofCrash(MonoBehaviour):

	def Awake():
		Debug.Log(typeof(Material))
*/

function Awake() {
	Debug.Log(typeof(Material));
}
